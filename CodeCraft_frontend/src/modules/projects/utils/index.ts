

/* export const normalizeProjectDates = (data) => {
  return {
    ...data,
    startDate: data.startDate === undefined ? null : data.startDate,
    dueDate: data.dueDate === undefined ? null : data.dueDate,
  }
} */